import 'package:flutter/material.dart';

class JJKT extends StatelessWidget {
  const JJKT({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text(
          'Testing',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}
